.col-sm-6.col-xs-12.col-lg-4.col-md-6
  .card
    .card-image-top.text-center
      = link_to product_path(product) do
        - product.images.each do |i|
          = image_tag i.pic.url(:thumb), class: 'img-fluid img-cover'
    .card-block
      .d-flex.justify-content-between.align-items-center
        %span.small
          = product.name
        #price
          = number_to_currency(product.price, precision: 0)
      %hr.my-2
      = link_to company_path(product.company) do
        .row.align-items-center
          #company-img
            = image_tag product.company.image.pic(:small), class: 'img-fluid rounded-circle', style: 'width: 50px; height: 50px;'
          #company-desc
            .small
              = product.company.name
            .small
              = total_rating_for product.company, readonly: true

